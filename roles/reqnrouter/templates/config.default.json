{
    "http": {
        "host": "{{listen_ip}}",
        "port": {{listen_port}},
        "domain": "{{domain}}"
    },
    "connector": {
        "ip": "{{connector_ip}}",
        "port": {{connector_port}},
        "url": "cdnhandler/ws"
    },
    "request_router": {
        "se_connection_limit": {{connection_limit}},
        "get_matching_sess_retries": {{get_matching_sess_retries}},
        "get_matching_sess_retry_delay": {{get_matching_sess_retry_delay}}
    },
    "logging": {
        "level": "{{loggin_level}}"
    },
    "api": {
        "port": 3000
    }
}
